# -------------------------------------------------------------------------------------------------
# Unreal host project (WagSaveDev) — generated artifacts and machine-specific files.
# The WagSave plugin lives in its own repo (symlinked into Plugins/) and is NOT committed here.
# -------------------------------------------------------------------------------------------------

# Unreal generated / build output
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# Template content — this host is a throwaway harness for developing/testing the plugin, so the
# sample assets are not versioned (regenerate from a template if needed). Commit only the project
# skeleton: Config/, Source/, and the .uproject. Remove this line if you want a fully reproducible host.
Content/

# Generated IDE / project files (regenerated from the .uproject)
.idea/
.vs/
.vscode/
*.sln
*.xcodeproj/
*.xcworkspace/
*.VC.db
*.VC.opendb
*.suo
*.sdf
*.opensdf

# UAT plugin-packaging staging (a throwaway HostProject + plugin copy, regenerated on package)
/WagSave/

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db

# Plugins developed elsewhere / regenerated:
#   WagSave  -> symlink into the standalone plugin repo (do not commit the symlink)
#   RiderLink -> generated by Rider if installed to the project
Plugins/WagSave
Plugins/RiderLink/
